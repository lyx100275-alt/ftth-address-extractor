# tests —— 回归语料与校验方法

本目录存放**脱敏后的竣工图语料**，作为解析器的回归基线。
语料只含代号（`a小区` / `b小区`），不含任何真实项目名、企业名、人名、联系方式。

## 目录

```
tests/
  README.md                 本文件
  corpus/
    a小区.dxf                 脱敏语料（源自一张实际 FTTH 竣工图）
    a小区.manifest.json       脱敏台账：逐条替换规则 + 命中数 + 校验结论
    a小区.probe.json          回归基线：对脱敏语料跑 probe 的产物
    b小区.dxf                 （未入包，见下「未入包语料」）
```

## 语料清单

| 代号 | 入包 | 体量 | 特征（回归覆盖点） | 来源 |
|---|---|---|---|---|
| `a小区` | 是 | 1.7 MB | 项目名内嵌在 MTEXT 格式串中；含楼层/户数标注、熔接与分纤箱标注 | 一张实际竣工图，已脱敏 |
| `b小区` | **否** | 143.5 MB | 超大体量、人防/建筑多专业混排、真实人名与联系人邮箱 | 一张实际竣工图，已脱敏 |

### 未入包语料：`b小区`

脱敏产物已生成并通过校验，但**体量 143.5 MB**，约为技能本体的 100 倍，
入包会显著拖慢技能同步与备份（平台 `.backup` 副本、git 仓库均会随之膨胀）。
故暂不入包，保留在工作区审计目录：

```
<workspace>/.audit/corpus_draft/b小区.dxf
```

如需纳入版本控制，建议单独存放于技能目录之外的语料库，而非技能包内。

## 脱敏规则（通用原则）

1. **只脱可定位到真实项目 / 企业 / 人的标识**；业务数据（楼栋 / 单元 / 层 / 户号 /
   FX 编号 / 光缆规格 / 几何坐标 / 排版参数）**一概不脱** —— 脱了语料就失去测量价值。
2. **词边界保护**：替换前必须校验命中处是否落在合法词边界上，否则会误伤非目标数据。
   已实测的失效案例：把 `1[3-9]\d{9}` 当手机号，命中浮点数值
   `0.49411764705882355` 中的 `17647058823`，**篡改图形数据**。故：
   - 中文串（项目名 / 企业名 / 人名）→ 命中处前后不得紧邻中文字符；
   - 数字串（电话等）→ 命中处前后不得是数字或小数点；
   - 仅自身足够特异的串（邮箱）可裸替换。
3. **长串优先**：规则按原串长度降序执行（`林荫书院项目` 先于 `林荫书院`）。
4. 不要求等长：DXF 是「组码行 / 值行」交替的文本格式，字节替换不改变组码结构，
   结构完整性由下述校验独立证明。

逐条替换明细见 `corpus/*.manifest.json`。

## 如何校验语料（三层，缺一不可）

**只比解析产物是不够的。** 上面的浮点误伤案例**不会**出现在任何文字值列表里 ——
只对拍 probe 产物会给出假的绿灯。必须做全量字节级校验：

1. **结构对拍**（证明只改了字面量，没动任何 DXF 结构）
   DXF 组码行 / 值行严格交替，故：
   - 行数必须相同；
   - 所有组码行（偶数索引）必须逐行完全一致；
   - 差异只允许出现在值行，且差异行数应等于替换处数。

2. **差异审计**（证明没有误伤）
   逐处定位每个替换命中，用词边界判据判定其是否合法；真误伤必须为 0。

3. **真机 probe 对拍**（证明语料仍可被技能正常解析，且测量结果不变）
   ```bash
   # 原图与脱敏图各跑一次，比对产物
   <python-with-ezdxf> scripts/ftth.py probe --dxf <corpus.dxf> --out <out.json>
   ```
   通过判据：两次均 rc=0；产物**键集合差异为 0**；
   同键值差异**只出现在被替换的字面量上**。

## 维护约定

- 新增语料必须**先脱敏、后校验、再入包**，三层校验全绿方可提交。
- 语料一旦入包即为**回归基线**，非必要不覆写；如需替换，同步更新对应
  `*.manifest.json` 与 `*.probe.json`，并在 `SKILL_CHANGELOG.md` 记录。
- 禁止把未脱敏的原始图纸放入本目录（本目录会随技能包同步 / 备份 / 分发）。
