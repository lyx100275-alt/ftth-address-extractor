# 标准地址表模板对照（外移自 SKILL.md，2026-09-16 第二次瘦身）

模板文件：`assets/标准地址表模板.xlsx`，Sheet 名「标准地址」，24 列（A~X），1 行表头 + 1 行示例数据，无合并单元格。

## 模板规则

标准地址模板已存放在 `assets/标准地址表模板.xlsx`，Sheet名"标准地址"，24列（A~X），1行表头+1行示例数据，无合并单元格。助手在生成成品时：

- **模板路径**：`assets/标准地址表模板.xlsx`（随skill分发，跨机可用）
- **格式严格跟模板**：列结构、列顺序、表头名称、地址写法、户号格式、分纤箱编号列位置、合并单元格方式、表头行数等，全部以模板为准。**表头须原样照抄** —— 模板表头尾部的 `(必填)` 是模板内容的一部分，**不得清洗或规整**：内部字段映射用的归一化不得作用于输出表头（实测某轮：二者复用一个函数，产物 5 个列名被悄悄改写、生成侧无告警，直到对产物回读才暴露）。出表后由出口断言拿**模板原文**逐列比对。
- **数据全部从DXF独立生成**：模板中的示例数据行仅用于展示格式，实际输出时替换为从DXF提取的真实数据，不得照搬模板中的任何数据
- **楼层列格式为中文**（`一层`/`二层`/`地下一层`）：`gen_addressbook.py --floor-format auto`（默认）**靠模板示例行的楼层单元格判定** —— 示例行给出中文样本即判为中文格式；示例行是占位文本（如 `<楼层>`）或空时 auto 无法判定，会退回 `3F` 形态，此时**必须显式传 `--floor-format chinese`**。改动示例行前先确认它仍能充当格式样本，不得清成占位。
- **列结构说明**：

  | 列 | 表头 | 内容 | 来源 |
  |---|---|---|---|
  | A | 分公司(必填) | 分公司名称 | 用户提供 |
  | B | 一级(必填) | 省 | 用户提供 |
  | C | 二级(必填) | 市 | 用户提供 |
  | D | 三级(必填) | 区 | 用户提供 |
  | E | 四级(必填) | 街道 | 用户提供 |
  | F | 五级 | 小区名称 | 用户提供 |
  | G | 五级沙盘ID | | 留空 |
  | H | 六级 | 楼栋（如1#楼） | DXF提取 |
  | I | 六级沙盘ID | | 留空 |
  | J | 七级 | 单元（如1单元） | DXF提取 |
  | K | 七级沙盘ID | | 留空 |
  | L | 八级 | 楼层（如一层） | DXF提取 |
  | M | 八级沙盘ID | | 留空 |
  | N | 九级 | 户号（如101） | DXF提取 |
  | O | 九级沙盘ID | | 留空 |
  | P | 分纤箱 | 分纤箱编号 | DXF提取 |
  | Q | 城郊属性 | | 留空 |
  | R | 别名 | 该行的别名写法 | 别名台账中状态为「已确认」的条目；**取值口径待项目方确认**（见下方注） |
  | S | 是否进线标识 | | 留空 |
  | T | 进线时间(yyyy-MM-dd HH:mm:ss) | | 留空 |
  | U | 进线类型 | | 留空 |
  | V | 沙盘临时表ID | | 留空 |
  | W | 区县 | 区县名 | **与 D 列「三级」同值**（`--addr` 第 3 段）；`HEADER_ALIASES` 已映射 `区县→区`，**不是留空列** |
  | X | 备注 | 交付说明性备注 | 可选 |

  > 注：**R 列「别名」的取值口径尚未裁定** —— 它位于沙盘对接字段段（城郊属性 / 进线标识 / 沙盘临时表ID …），
  > 与「归一化原始异写」是否同一语义**无客观判据**，故按 [operations_discipline.md](operations_discipline.md)
  > §5.3 第④类处理：**未获裁定前不得写入**，只把候选列进待确认项。
  > 现状：`gen_addressbook.py` 的 `HEADER_ALIASES` **未映射** `别名` / `备注`，两列恒为空（留空是当前唯一正确行为）。
